# Global Budget
total_budget_seconds: 585  # 10 minutes (600s) minus 15s safety buffer
clip_budget_seconds: 45

# Parallelism
max_workers: 2

# Models
vision:
  model_id: "accounts/fireworks/models/kimi-k2p6"
  fallback_model_id: "accounts/fireworks/models/qwen3p7-plus"
  temperature: 0.0

text:
  model_id: "accounts/fireworks/models/glm-5p2"
  temperature: 0.0

# Timeouts & Retries
timeouts:
  ffprobe: 20
  ffmpeg: 20
  primary_vision: 18
  fallback_vision: 8
  text_generation: 20

retries:
  max_attempts: 3
  backoff_factor: 0.5


